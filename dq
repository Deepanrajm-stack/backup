
json_cols_to_rename = [col_name for col_name in dq_mismatch_df.columns if col_name in df_rep.columns and col_name != "DQ_Rule"]
for col_name in json_cols_to_rename:
    dq_mismatch_df = dq_mismatch_df.withColumnRenamed(col_name, f"json_{col_name}")
